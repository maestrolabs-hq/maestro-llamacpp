# maestro-llamacpp

maestro-llamacpp is the estate's model router. It supervises llama.cpp server
processes and exposes one OpenAI-compatible endpoint per model plus a generic
routing endpoint.

Status: founding specification only, no implementation yet.

See the [model router design](docs/superpowers/specs/2026-09-03-model-router-design.md).
