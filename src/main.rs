//! The `model-router` binary: reads a catalog of llama.cpp models and, once
//! the later slices land, supervises one server process per model.

fn main() {
    eprintln!("model-router: no commands implemented yet");
    std::process::exit(1);
}
